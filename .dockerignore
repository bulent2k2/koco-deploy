# Derleme bağlamına girmesine gerek yok — .git LFS nesneleriyle birlikte
# yüzlerce MB olabilir ve HF Space derlemesini gereksiz yavaşlatır.
.git
.gitattributes
.gitignore
*.log
.DS_Store
README.md

# Dağıtım öncesi deneme takımı: imaja HİÇ girmiyor (Dockerfile yalnız adı geçen
# dosyaları COPY'liyor, `COPY .` yok) ama derleme bağlamına giriyordu -- 61 MB'ın
# çoğu ekran görüntüsü (sonuclar/) ve playwright (tarayici/node_modules).
once_dene/
